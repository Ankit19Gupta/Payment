/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone", // Creates optimized production build
  // If you need static export instead, use:
};

module.exports = nextConfig;
